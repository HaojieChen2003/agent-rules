# Agent Rules — 个人领域规则管理器

本规则库由用户通过绝对路径显式指定后激活。激活时必须完整读取本文件，再按任务语义读取命中的领域框架；不得只凭文件名猜测规则，也不得绕过已命中的框架。

## 激活与作用域

1. 本文件是当前任务的最高用户级领域准则，但不覆盖系统、开发者、安全或平台约束。
2. 用户当前任务的明确要求可覆盖本规则的默认策略；不得用框架或外部 Skill 否定用户的明确目标。
3. 本规则在当前任务中持续生效，直到用户明确撤销、替换或任务结束。
4. 领域框架只约束对应子任务，不把其他领域规则机械套入当前任务。
5. 框架引用源 Skill 时，本规则决定目标、边界和验收；源 Skill 提供执行流程、脚本或参考知识。

## 技能清单

| 文件路径 | 领域与作用 |
|---|---|
| `/Users/eiravale/Desktop/Agent Rules/Calibre书库管理.md` | Calibre 书库、电子书元数据、格式、封面与批量写库 |
| `/Users/eiravale/Desktop/Agent Rules/微信读书Skill.md` | 微信读书搜索、评分、书架、笔记、阅读统计与推荐数据源 |
| `/Users/eiravale/Desktop/Agent Rules/Latex排版助手.md` | 中文 LaTeX 工程、学位论文、编译、引用、格式与终检 |
| `/Users/eiravale/Desktop/Agent Rules/数学工作者.md` | 数学概念、解题、推导、证明、数值验证、训练与数学笔记 |
| `/Users/eiravale/Desktop/Agent Rules/Coder.md` | 编码、代码审查、技能与插件开发、官方文档与位图生成 |

具体触发与排除条件以各框架 frontmatter 的 `when_to_use` 和 `do_not_use` 为唯一事实来源。判定可能命中时，先读取对应框架再仲裁。

## 指令优先级

在系统和平台约束之下，按以下顺序执行：

```text
用户当前任务的明确要求
> 本规则中标记为“硬性”的领域边界
> 项目已有结构、接口与约定
> 本规则的默认策略
> 外部 Skill 的默认建议
> agent 的一般偏好
```

硬性边界用于防止数学错误、数据损坏、隐私泄露和不可逆操作；默认工具、输出深度、配色或模板均可被用户要求和项目现状覆盖。

## 路由规则

1. 主框架由任务的核心正确性风险决定，不由当前目录、文件扩展名或所用工具单独决定。
2. 一个任务只设一个主框架，可有至多两个辅助框架。主框架决定目标、主流程和验收；辅助框架只处理其领域内的子任务。
3. 数学结论、推导、证明或以验证数学命题为目的的代码，以 `数学工作者.md` 为主。
4. `.tex` 工程的编译、格式、引用、论文结构与版面，以 `Latex排版助手.md` 为主。
5. 数学内容写入 `.tex` 时，数学工作者负责内容正确性，LaTeX 助手负责写入、编译和产物验收。
6. Python 是交付软件或工程模块时，以 `Coder.md` 为主；Python 仅用于数学验证时，以数学工作者为主。
7. 书库写入、格式删除和元数据变更，以 `Calibre书库管理.md` 为主；微信读书只作为配套数据源，除非任务本身就是微信读书操作。
8. 框架未覆盖的场景按用户要求和一般规则执行，不随意套用相近框架。

混合任务通常按以下顺序执行：

```text
领域内容 → 产物实现 → 运行或编译 → 跨领域统一验收
```

## 总体约束

1. **避免危险操作**：执行删除、覆盖、移动、批量重命名、数据库批量写入等操作前，先解析并核对精确目标；能建立恢复点时先备份。禁止对宽泛路径使用破坏性递归命令，禁止先删后建。
2. **自主执行常规工作**：硬性边界、验收条件和顺序依赖必须遵守；具体实现、工具选择与非关键步骤由 agent 自主决定，不必逐步请示。
3. **只在必要时提问**：缺失信息会实质改变结论、产物方案代价明显不同、操作不可逆，或发现超出框架预期的重大问题时才暂停询问。复杂本身不是暂停理由。
4. **文件产出**：用户未要求创建、修改、编译或导出文件时，只在回复中给结果。用户已明确要求操作指定文件时视为授权，不重复确认；删除、整体覆盖或结构性重写仍按危险操作处理。
5. **最小改动**：只修改任务必要的文件，保留用户现有改动、项目接口、符号约定和设计语言，不顺带重构无关内容。
6. **证据与不确定性**：不把猜测写成事实；无法验证时说明已确认内容、未确认内容及所缺条件。
7. **验证失败处理**：优先定位第一个阻断性根因，修复必要的连锁问题后重新验证，不用无关改动掩盖错误。
8. **显式定义先行（硬性）**：非标准符号、函数、Green 核、算子、矩阵、系数和缩写在首次进入核心公式前，必须给出参数、定义域和数学定义。若已有可直接计算的显式表达式，必须在首次使用前完整写出；不得仅用“由某文献给出”、后置交叉引用或未解释的名称替代。表达式较长时可独立成节，但顺序仍须保持“定义 → 使用”。

## 危险操作分级

| 级别 | 通用操作 | 要求 |
|---|---|---|
| 🔴 必须确认 | 删除、移动或不可恢复地覆盖用户原始文件；清空目录；大范围结构性重写 | 先说明精确目标、影响与恢复方案，等待用户确认 |
| 🟡 建立恢复点 | 批量写入、修改数据库、大范围自动修复、可能覆盖既有产物的重生成 | 先备份或建立可验证的恢复点，再执行并报告 |
| 🟢 自主执行 | 只读检查；不改变用户状态的查询；用户已明确要求且可恢复的常规编辑；独立临时目录中的验证 | 直接执行，按风险报告结果 |

领域特定的危险操作和例外必须写入对应框架，不在本表中硬编码业务字段。

## 临时文件与项目产物

1. 任务外的临时脚本、下载缓存、截图和一次性分析产物，放入 `/Users/eiravale/Desktop/临时文件/<任务唯一目录>/`。
2. 不得直接混用临时文件根目录；任务结束时只清理本任务创建且已核对精确路径的临时子目录，不删除其中的用户自有内容。
3. 项目构建文件遵循项目已有工具链和输出目录。LaTeX 的 `.aux`、`.bcf`、`.bbl`、`.toc`、SyncTeX 等不因本规则被强制搬离项目。
4. 正式交付文件、用户要求保留的数据和可复现实验代码放入项目目录或用户指定目录，不属于临时清理范围。

## 框架文件约定

每个领域框架至少包含：

```text
frontmatter     when_to_use / do_not_use / version / last_updated
定位            领域目标与排除范围
任务路由        场景 → 必读参考 → 最低验收
核心流程        必须遵守的顺序依赖
工具与环境      可执行入口和依赖
边界            禁止事项与跨框架接口
```

详细规则放入 `references/` 并由框架直接说明何时完整读取；固定回归案例放入 `evals/`。避免在入口和参考文件中重复维护同一规则。

## 版本控制（git）

规则库已纳入 git 管理（2026-08-15 初始化，仓库根目录 `/Users/eiravale/Desktop/Agent Rules/`，初始提交 f3f7b35，13 文件 1496 行；仓库级 user.name=eiravale，不动全局配置）。远程仓库：`https://github.com/HaojieChen2003/agent-rules`（公开，origin/main），由 gh CLI 管理。

1. **改动必提交**：每次修改规则文件后，必须 `git add` 相关文件并 `git commit`。commit message 格式：`<框架>: v<版本> <改动摘要>`（如 `math: v2.1.0 新增最后结果原则`）。
2. **增量读取协议（速度关键）**：agent 读取规则前先 `git log --oneline` 检查自上次读取后的变更；有变更时只读 `git diff <上次版本> HEAD` 的差异行，其余用记忆缓存，不全量重读。
3. **回滚**：单文件还原 `git checkout -- <文件>`；撤销某次提交 `git revert <版本>`；需要时查看历史 `git log --oneline`。
4. **版本号与提交对齐**：frontmatter 的 `version` / `last_updated` 变更必须伴随一次 commit，二者不得脱节。
5. **推送需用户同意**：`git commit` 在本地可随时执行；`git push` 到 origin **必须先征得用户明确同意**（说明本次推送内容与目的），未经同意不得推送。多设备同步以 origin/main 为准，拉取 `git pull` 不受此限。
6. 规则库内容以 git 记录为准，md 文件本身是工作副本；远程为公开仓库（2026-08-15 由私有改为公开，用户明确要求）。

## 维护约定

1. 新增领域框架时登记到技能清单，并添加完整 frontmatter。
2. 修改框架内容时更新 `version` 与 `last_updated`；修改触发、排除或跨领域边界时，同时检查本文件的路由是否仍一致。
3. 删除框架或用户文件前必须确认；废弃引用应先验证替代路径可用。
4. 框架依赖源 Skill 或脚本时，写明实际路径和运行环境；源内容更新、移动或删除后同步检查引用，防止死路径。
5. 只有同时满足“跨任务可复用、会改变流程或验收、不与现有规则重复、已有案例支持”时，才在任务结束时集中建议沉淀新规则。未经用户确认不得自动修改本规则库。
