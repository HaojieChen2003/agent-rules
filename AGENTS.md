# Agent Rules — 个人领域行为准则

本规则库由用户通过绝对路径显式指定后激活。激活时完整读取本文件，再按任务语义读取命中的领域框架；不得只凭文件名或扩展名猜测规则。

## 一、作用域与优先级

本规则库提供个人领域偏好、任务路由、安全边界和验收要求，不覆盖系统、平台或安全约束。

在这些约束之下，按以下顺序执行：

```text
用户当前任务的明确要求
> 领域框架中的硬性安全或正确性边界
> 项目已有结构、接口与约定
> 本规则的默认策略
> 外部 Skill 的默认建议
> agent 的一般偏好
```

用户要求可以覆盖默认工具、输出深度、模板和风格；不得用框架或外部 Skill 否定用户的明确目标。硬性边界只用于防止实质性错误、数据损坏、隐私泄露和不可逆操作。

## 二、领域框架

| 框架 | 核心任务 |
|---|---|
| `Calibre书库管理.md` | 电子书库、元数据、格式、封面与批量写库 |
| `Latex排版助手.md` | LaTeX 工程、编译、引用、格式与视觉验收 |
| `数学工作者.md` | 数学概念、推导、证明、数值验证与数学笔记 |
| `Coder.md` | 编码、代码审查、Skill 与插件开发、官方文档和位图生成 |

各框架 frontmatter 的 `when_to_use` 和 `do_not_use` 是本规则库的路由依据。判定可能命中时，先读取对应框架，再决定是否适用。

### 路由原则

1. 按任务的核心正确性风险选择一个主框架，不按当前目录、文件扩展名或所用工具机械判断。
2. 一个任务只设一个主框架，可有至多两个辅助框架。主框架决定目标、主流程和验收；辅助框架只处理其领域内的子任务。
3. 数学结论、推导、证明或以验证命题为目的的代码，以数学框架为主。
4. LaTeX 的编译、格式、引用和版面，以 LaTeX 框架为主；数学内容写入 `.tex` 时，数学框架负责内容正确性，LaTeX 框架负责安全写入、编译和产物验收。
5. Python 是软件交付物时，以 Coder 为主；Python 仅用于数学验证时，以数学框架为主。
6. 电子书库写入、格式删除和元数据变更，以 Calibre 框架为主。
7. 框架未覆盖的任务按用户要求、项目约定和通用准则执行，不随意套用相近框架。

混合任务通常按以下顺序完成：

```text
领域内容 → 产物实现 → 运行或编译 → 跨领域统一验收
```

## 三、通用行为准则

1. **明确任务合同**：识别目标、输入、限制、允许的修改范围和完成条件；缺失信息会实质改变结果时才询问，复杂本身不是停止理由。
2. **最小必要改动**：只修改任务所需内容，保留用户已有结构、接口、设计语言和无关改动，不顺带重构。
3. **区分任务类型**：问答、分析、诊断和审查默认只读；用户明确要求创建、修改、编译或导出时才实施相应写操作。
4. **证据与不确定性**：不把猜测写成事实；无法验证时区分已确认内容、合理推断、未知条件和所需证据。
5. **保护用户数据**：删除、覆盖、移动、批量重命名和数据库写入前，解析并核对精确目标；高风险操作应具有可验证的恢复方式。
6. **验证与风险相称**：运行或编译成功不自动代表内容正确；按任务风险检查结果、接口、引用、版面或数据状态。
7. **专业对象先说明**：非标准对象应在首次关键使用前给出足以支持当前任务的定义；具体完整程度由主框架规定。
8. **临时与正式产物分离**：一次性脚本、缓存、截图和诊断产物放入任务独立临时目录；结束时只清理本任务创建且已核对的内容。

## 四、操作风险

| 级别 | 典型操作 | 默认行为 |
|---|---|---|
| 高 | 删除或移动原始文件、清空目录、不可恢复覆盖、大范围结构重写 | 先核对目标、影响与恢复方式；授权不明确时询问 |
| 中 | 数据库或批量写入、大范围自动修复、覆盖既有产物的重生成 | 先建立恢复点或可验证的回滚方式 |
| 低 | 只读检查、查询、用户已明确要求且可恢复的常规编辑、独立临时目录中的验证 | 自主执行并按风险报告 |

不得对宽泛或未解析的路径执行破坏性递归操作，不得以“先删除再重建”代替安全修改。领域特定风险写入对应框架。

## 五、框架与参考资料

领域框架至少说明：

```text
触发与排除条件
领域目标与边界
任务路由
稳定的决策原则
最低验收标准
需要按需读取的参考资料
```

具体命令、接口字段、事故记录、软件版本行为和项目经验优先放入 `references/`、`evals/`、脚本或项目级规则，不进入全局行为准则。只有跨任务可复用、会改变决策或验收、与现有规则不重复且有案例支持的内容，才应沉淀为长期规则。

## 六、规则库维护

规则库使用 Git 管理。修改规则时保持变更主题单一、更新相应版本信息并进行本地验证。具体提交、回滚、同步和 GitHub 操作见 `GitHub操作手册.md`；推送远程仓库属于外部状态变更，必须得到用户明确授权。
