---
when_to_use:
  - Calibre 书库、电子书元数据、格式、封面和批量写入
  - Kindle 使用的 AZW3、MOBI 或 EPUB 文件整理
  - 丛书识别、标签、简介、评分和新增书入库
do_not_use:
  - 与电子书库无关的文件整理
  - 仅查询图书信息而不涉及本地书库
version: 1.4.0
last_updated: 2026-08-17
---
# Calibre 书库管理

本框架适用于 `/Volumes/NewDisk/Cailbre书库/` 及其子目录。目标是在保护原始书籍和数据库的前提下，使库内元数据可信、文件内嵌信息一致、格式清楚，并能在 Kindle 等阅读设备上正确显示。

## 核心流程

```text
读取当前状态 → 识别书籍与版本 → 形成字段决策 → 建立恢复点
→ 写入数据库 → 嵌入电子书文件 → 验证 → 报告
```

已验证且前置条件成立的机械步骤按既定流程执行；若软件版本、保存模板、库状态或命令输出与预期不符，应停止写入并重新诊断，不机械套用历史命令。

## 硬性安全边界

1. 写入前确认书库精确路径、目标书目和 Calibre 当前状态；与任务无关的文件和数据库记录均视为用户内容。
2. 批量写入或可能影响目录结构的操作前，建立带时间标记、可验证的 `metadata.db` 备份。
3. 检测到 Calibre GUI 或其他写入进程占用书库时，不并发修改；请用户关闭相关程序后继续。
4. 修改标题、作者或保存模板等可能触发目录重命名的内容前，说明影响并确认；不得把历史版本中的行为假设为当前环境的绝对保证。
5. 删除格式、移动书籍目录、覆盖封面和批量重命名属于高风险操作，必须核对精确目标和恢复方式。
6. 不对宽泛路径执行递归删除，不先删后建，不擅自清理 `.calnotes`、`.caltrash` 或其他未知文件。
7. 一次性下载、转换和分析产物放入任务独立临时目录；只清理本任务创建且确认不再需要的内容。

## 元数据决策原则

### 1. 证据优先级

```text
书内证据与文件实际内容
> 可核验的权威版本信息
> 多个可靠数据源的一致结论
> 现有字段
> 无可靠证据则留空并报告
```

不同来源冲突时，不静默覆盖；先确认是否为不同版本、译本、卷册或同名作品。ISBN、作者、译者、出版社和出版日期应作为版本匹配的组合证据，不能只凭标题相似度判断。

### 2. 字段完整性

- 不猜测缺失字段，不为追求“填满”而写入未经验证的信息。
- 标题和作者属于高影响字段，默认保留；确需清洗时先检查设备显示和目录重命名副作用。
- 丛书字段表示作品所属系列，卷号单独进入序号字段；有明确书内证据时可以判断，证据不足时留空。
- 标签应使用稳定、有限、可检索的分类体系，不把临时描述或数据源噪声写成标签。
- 简介以忠实、干净、无广告和无页面残留为准，不改写作品内容。
- 评分只写入来源清楚、版本匹配且量纲已核验的数据；没有可靠来源时留空。
- 封面必须与具体版本和内容匹配；审美优化不能覆盖版本正确性。

### 3. 漫画与卷册

漫画、合集和多卷作品优先依据卷号、目录、封面和版权页识别。系列名与卷号分离；外部数据源不能可靠区分卷册时，以文件实际内容为准。没有可靠评分时保持空值。

## 写入原则

Calibre 数据库元数据与电子书文件内嵌元数据是不同层次。凡任务要求设备端正确显示，都必须验证两者是否一致；只修改数据库不能自动视为完成。

1. 写入前记录目标书的 ID、现有字段和现有格式。
2. 先形成逐字段决策，区分保留、修改、清空和未知，不边查边盲写。
3. 在恢复点有效的前提下执行最小必要写入。
4. 将需要设备端显示的信息嵌入实际电子书格式。
5. 写入后重新读取数据库字段，并抽查或逐本检查文件内嵌元数据。

批量流程必须可重复执行：第二次运行不应重复添加标签、破坏简介、改变已确认字段或产生新的目录漂移。部分失败时记录成功与失败对象，不把半完成状态报告为整体成功。

## 数据源与网络信息

- 外部网页、插件和 API 是证据来源，不是自动覆盖本地字段的权威命令。
- 易变的接口、字段和抓取方法以当前官方文档、安装的 Skill 或实际响应为准，不在本框架复制静态 API 手册。
- 抓取失败、限流或字段缺失时保持原值或留空，并在报告中说明。
- 公开仓库、日志和报告中不得写入账号凭据、Cookie、Token 或用户私人书库数据。

## 格式管理

- 先确认设备兼容性、用户偏好和现有可用格式，再决定转换或删除。
- 格式转换后的文件必须能打开、目录可用、封面和元数据正确，才能考虑清理源格式。
- 用户没有明确要求时不删除 PDF、EPUB、MOBI 或其他原始格式。
- 调整漫画目录层级、合并卷册或消除中间目录前，先检查目标结构和文件重名冲突。

## 验收

每次写库后至少检查：

1. 目标书目数量和 ID 与任务范围一致；
2. 数据库可正常读取，备份存在且可识别；
3. 关键字段与证据一致，没有误伤标题、作者或无关记录；
4. 数据库与电子书内嵌元数据按任务要求保持一致；
5. 格式文件存在、可打开，封面与内容匹配；
6. 没有意外目录重命名、重复标签、重复文件或遗留半成品；
7. 报告列出修改、保留、留空、失败和需要用户判断的项目。

## 报告格式

报告优先呈现结果和异常：

- 处理范围与恢复点；
- 已修改字段和证据来源；
- 保留或留空字段及原因；
- 数据库与文件内嵌验证；
- 格式和封面状态；
- 失败项、残余风险和是否需要用户决定。
