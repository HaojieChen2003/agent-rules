---
when_to_use: Calibre/书库/电子书/Kindle/azw3/mobi/元数据/丛书/标签/简介/封面/评分/删PDF/新增书处理/漫画目录整理
do_not_use: 与书库无关的日常任务（旅行、问答等）
version: 1.3.4
last_updated: 2026-08-16
---
# Agent Rules — Calibre 书库

本文件适用于 `/Volumes/NewDisk/Cailbre书库/` 及其子目录。

本库是用户的 **Kindle 书库**：元数据完整（丛书/标签/简介/封面/评分）、格式干净（以 azw3 为主）、拷入 Kindle 后显示正确。

## 〇、核心心智

一条主线：**新增书 → 识别内容（读进去）→ 填元数据 → 双写入库 → 验收报告**。

元数据质量是 Kindle 显示正确性的唯一来源，其中 `series`（丛书）直接决定 Kindle 自动分类——**丛书填对了，书库自动整洁，无需手动建收藏夹**。因此丛书识别是本规则中唯一要求"先读懂书、再下判断"的字段。

## 〇·一、执行原则（直来直去）

- 本文件是**确定性指令**：每步都写明"做什么 → 怎么做 → 产出什么"，机械步骤按固定命令执行，**不许现场发挥**。
- 全书只有两处允许判断：丛书识别（三.3，含漫画系列）、封面审美（三.10）。其余字段一律按既定规则执行。
- 任何数据源未命中的字段 → **留空**，写入报告，不猜测、不编造。
- 不确定是否要操作时，停下来问用户，不擅自扩大范围。

## 一、通用规则

### 1. 指令优先级

1. 用户当前任务中的明确要求；
2. 本库已有的结构、模板和约定；
3. 本文件中的默认规则；
4. 外部 Skill 提供的通用建议。

不得使用 Skill 或模板覆盖用户的明确要求。修改本库时，优先保持其现有结构（Calibre 标准库结构：作者目录 → 书名目录 → 书文件/封面/OPF）。

### 2. 工具与执行边界

使用 `calibredb`（库操作）、`ebook-meta` / `embed_metadata`（文件内嵌元数据）、Python（批量抓取与处理）、curl（网络请求）、`sips`（图片分辨率），不引入无关语言或框架。

得到明确做法后直接执行，不反复推敲已确定的内容。若缺少的信息会实质改变结果，应停止并向用户提问。

### 3. 文件产出

| 场景 | 行为 |
|---|---|
| 一般问答、思路或概念解释 | 仅文字回复，不创建文件 |
| 用户明确要求输出文档或报告 | 创建用户指定格式；未指定时使用兼容 Typora 的 Markdown |
| 需要向用户交付检查清单或报告 | 以 Markdown 交付，兼容 Typora |

仅修改任务所需文件，不顺带重构无关内容。

### 4. 文件操作安全

- 移动、删除文件或目录前，必须先检查源与目标。禁止先删后建，禁止对宽泛路径使用 `rm -rf`。
- 操作 `metadata.db` 前，必须先在书库根目录创建备份：`cp metadata.db metadata.db.bak_日期`。
- 工作区内已有但与当前任务无关的文件（如 `.calnotes`、`.caltrash`）均视为用户内容，必须保留。
- 任务产生的一次性中间文件放入 `/Users/eiravale/Desktop/临时文件/<任务唯一目录>/`；任务完成后只删除本任务创建且已核对精确路径的临时子目录，不删除临时文件根目录或其中的用户自有内容。

## 二、铁律（本库特有，违反会造成不可逆影响）

### 1. ⚠️ `calibredb set_metadata` 会触发“保存到磁盘”模板重写

任何 `set_metadata` 写操作都可能触发 Calibre 按保存模板重写全库目录结构（作者目录、书名目录按拼音规范化）。**后果不可回滚**（本库曾因此整体重命名）。

规则：

- 默认**不修改** `title` 和 `authors` 字段；
- 确需修改前，必须向用户说明重命名影响，获得确认后执行；
- 修改 `series` / `tags` / `comments` / `publisher` / `pubdate` / `identifiers` / `cover` / `rating` 等字段安全，不会改动目录（前提：目录已是规范化格式）。

### 2. 操作前必须备份，操作时 Calibre GUI 必须关闭

- 批量写库前：备份 `metadata.db` 到 `metadata.db.bak_日期`。
- 检测到 Calibre 主程序正在运行时，`calibredb` 会被锁拒绝。先告知用户关闭 Calibre，再继续。

### 3. 双写原则

Kindle 显示的是 **azw3/mobi 文件内嵌元数据**，不是 Calibre 库数据。

- `calibredb set_metadata`：更新库（Calibre GUI 浏览）。
- `calibredb embed_metadata`：把库元数据+封面嵌入书籍文件（Kindle 实际显示）。
- **两处都要写**，缺一不可。封面替换用 `--field=cover:路径`（会更新库内 cover.jpg，随后需 `embed_metadata` 嵌入文件）。

## 三、元数据写入策略（本 agent 的核心职责）

本 agent 的职责是写入内置标准字段（`title` / `authors` / `author_sort` / `comments` / `cover` / `series` / `series_index` / `tags` / `publisher` / `pubdate` / `identifiers` / `languages` / `rating`）。写入 = **验证 + 理解 + 决策**，不是数据搬运。

### 1. 验证层级（字段冲突时按此优先级裁决）

```text
书内证据（封面题名/书内页/文件内嵌元数据） > 豆瓣与微信读书核对 > 现有字段 > 数据源未命中则留空
```

### 2. 验证动作（每本书必做）

1. `calibredb list` 读现有字段，明确“已有什么”；
2. 豆瓣抓取权威版本信息（suggest 选版本 → 详情页：出版社/日期/ISBN/丛书/简介/评分）；
3. 微信读书抓评分（接口与算法详见《微信读书Skill.md》）；
4. **读封面**，确认封面与书匹配（本流程曾抓出：id=41《图解国富论》错配《国富论》全译本封面；id=57 封面为巴金《爱情三部曲》，作者字段却填了夏目漱石）；
5. 综合判断 → 写入 → `calibredb list` 核对 + `ebook-meta` 抽查文件内嵌。

> **漫画例外（用户明确要求）**：漫画类书籍**不做外部数据源写入**——跳过步骤 2（豆瓣抓取）与 3（微信读书评分），`rating` 一律留空。其元数据基于文件名/书名与内容判断（丛书按三.3c，标题按三.7，标签按三.8），封面直接采用书内封面（不走豆瓣大图流程）。

### 3. 丛书识别规则（唯一要求"先读书、再判断"的字段）

丛书不是"查豆瓣有没有这个系列"的查询题，而是**读完这本书、判断它属于哪个系列**的判断题。不要死板依赖网上现成分类（豆瓣丛书字段经常缺失或不准），**以书的内容证据为准，自己归类**。

#### 3a. 总原则

- **内容优先，自行归类**：只要书的内容证据足以说明它属于某系列，就填，不必等豆瓣给你答案；
- 丛书名用**系列名**（不带卷册号、不带"系列"字样），卷号单独进 `series_index`；
- Kindle 自动分类依赖本字段，填对 = 书库自动整洁（用户因此**明确要求**积极利用本字段，不要因为"不确定"就一律留空）；
- 但判断要有依据，能自圆其说；实在看不出系列归属才留空，不编造。

#### 3b. 判断依据（按优先级取）

1. **内容证据**（最强，优先于一切外部数据源）：
   - 书名卷册结构：`系列名 + 卷册号`（如"海贼王·第1卷""射雕英雄传·一"）→ 系列名即书名主体；
   - 简介/前言/版权页明确说明所属系列（如"本书为 XX 系列第 N 部"）；
   - 同一作者、同一世界观/人物的系列作品（金庸武侠、刘慈欣科幻、阿加莎波洛系列）；
   - 三部曲/四部曲等结构化系列（如"爱情三部曲"）。
2. **标题括号线索**：书名括号内含系列名且非版本描述（"日知文库""果麦经典""磨铁经典"）→ 丛书名；
3. **豆瓣丛书字段**：仅作**佐证**，不是唯一来源。豆瓣没有 ≠ 不能归；豆瓣有但与内容证据冲突时，以内容证据为准。

#### 3c. 漫画系列（用户明确要求）

同一系列的漫画多卷本 → 一律按丛书处理：

- `series` = 系列名（如"海贼王""进击的巨人"），不带卷册后缀；
- `series_index` = 卷号数字，从书名"第X卷 / Vol.X / 第X册 / 01"等识别；识别不到不硬猜，留给 Calibre 默认；
- 目的：Kindle 按丛书自动归类整套漫画，无需手动建收藏夹；
- 单行本 / 无系列的短篇 → 留空；
- 漫画整体不走外部数据源（不抓豆瓣/微信读书、不写评分），见三.2 漫画例外。

#### 3d. 留空条件

- 单本独立作品（无系列）；
- 内容证据不足、外部数据源也无提示，无法自圆其说 → 留空，报告说明，不编造。

### 4. 逐字段策略

| 字段 | 决策逻辑 | 验证方式 |
|---|---|---|
| `title` 书名 | 按三.7 标题清洗规则执行 | 封面题名 + 豆瓣对比；改前遵守二.1 重命名规则 |
| `authors` 作者 | 只写真实作者，多作者用 `&` 分隔；**清洗脏数据**（“【日】太宰治,徐建雄,ePUBw.COM”→“太宰治”；“未知”→“尼古拉·奥斯特洛夫斯基”）；译者不进作者字段 | 封面署名 + 豆瓣作者栏；可疑时读封面确认 |
| `tags` 标签 | 中文 3 个：第 1 个=大类（经济学/外国文学/健康/科普/武侠小说…），第 2-3 个=主题或作者（行为金融/金庸/刘慈欣…）；不用“经典”“精选”空泛词 | 书名 + 简介内容推断；不照抄豆瓣英文分类 |
| `comments` 简介 | 豆瓣简介只当**事实素材**，产出必须重写：现代视角、客观、讲清“内容+价值+适合谁”，70-140 字，禁营销腔/编辑推荐/版本广告/作者生平堆砌 | 抓豆瓣简介 → 清洗（见三.9）→ 重写 |
| `publisher` 出版方 | 豆瓣优先（更规范：“广州出版社”优于“广州朗锐数字传媒科技”）；现有 ISBN 与豆瓣同版本时保留现有 | 豆瓣出版社 + ISBN 版本比对 |
| `pubdate` 日期 | 现有合理日期（非 0101-01-01）优先；缺失或脏数据用豆瓣；统一 `YYYY-MM-DD` | 校验年份在 1900-2100 |
| `identifiers` / `isbn` | 现有 ISBN 优先（**版本指纹**）；缺失补豆瓣；**版本对不上就不硬填**（留空并写入报告） | ISBN 与书名/出版社一致性检查 |
| `languages` 语言 | 按内容语言判断：中文 `zho`、英文 `eng` | 现有字段 + 书名/内容语言 |
| `rating` 评分 | 按三.6 评分回退链：微信×0.7+豆瓣×0.3 → 仅豆瓣 → 网络搜索 → 留空；**漫画不适用，留空** | 微信读书 + 豆瓣 + 网络搜索兜底 |
| `cover` 封面 | 按三.10 封面审美标准执行；必须是**出版书真实封面** | 读图核验内容 + `sips` 查分辨率 |

### 5. 不写入的字段

- `pages`：难以准确，不写；
- 自定义列：本库未建，不写。

### 6. 评分字段规则（rating）

`rating` 按《微信读书Skill.md》的算法计算：`round(微信读书分 × 0.7 + 豆瓣分 × 0.3, 1)`，10 分制保留一位小数。数据源、字段解析、版本匹配与缺失处理，详见同目录《微信读书Skill.md》。

**⚠️ 写入语法（本库实测）**：`calibredb set_metadata --field=rating:N` 的 **N 是 0-5 星标度（×2 存储为 0-10）**，不是 0-10 分！传入 0-10 分（如 6.4/9.2）会被 clamp 成 10。**正确写法：`--field=rating:{分数/2}`**（如豆瓣 9.2 → 写 4.6 → 显示 9；豆瓣 6.4 → 写 3.2 → 显示 6）。库内显示为整数（round half up）。

**评分回退链**（数据源缺失时按序降级）：

1. 微信读书 + 豆瓣都有 → 加权公式；
2. 微信读书无、豆瓣有 → `rating` = 豆瓣分；
3. 两者皆无 → 网络搜索可核实的评分（其他平台），写入并在报告注明来源与口径；
4. 仍无 → 留空，报告说明。

漫画类书籍不适用（不走数据源，见三.2 漫画例外），`rating` 一律留空。

### 7. 标题清洗规则

- **保留**：主书名 + 有信息量的副标题（“寻路中国**：从乡村到工厂的自驾之旅**”——冒号后是内容一部分）；卷册号（射雕英雄传·一）。
- **去掉**：括号内营销语/版本广告/推荐语（“窄门(诺贝尔文学奖得主…)(果麦经典)”→“窄门”）；来源站名（ePUBw.COM）；编辑推荐文案。
- **繁体转简体**。
- **繁体检测用严格繁体专用字集**（醫護學體們來時與為國動變過還進讓開關輕謝等），排除"告/细/胞"等简体同形字，避免误报（"告诉你/细胞"均为简体）。
- 改 `title` 前遵守二.1 重命名规则。

### 8. 标签命名规范

- 全中文；
- 第一标签=类别（经济学/外国文学/健康/科普…），第二、三标签=具体主题或作者（金庸/太宰治/刘慈欣/行为金融…）；
- 不使用“经典”“精选”等空泛词。

### 9. 简介清洗规则

- 截断到“目录”字样之前（豆瓣简介常混入目录）；
- 用 `html.unescape` 解码 HTML 实体；
- 去除 `<style>`/`<script>`/`<p>` 等标签残留；
- 长度 70-140 字为宜，超长截断。

### 10. 封面审美判断标准

用户偏好：**平面、有设计感、现代版式**的出版书真实封面。

- **保留**：现代极简、扁平色块、几何/插画构图、经典文学的现代版式（果麦/新经典风格）、与书内容一致的摄影设计。
- **替换**：老版扫描件/书影、作者照片或讲座式封面、淘宝式拼贴（地图+装饰元素堆叠）、仿 3D 书脊、纯文字无设计、营销标签堆砌、低清（<400px）、与书不符（错封面）。
- 替换来源：豆瓣 `l/` 大图（**必须是出版书真实封面，不用 AI 生成**）。
- 判定需读图：批量筛选用拼图法（每批 10 张拼一张，读图标记），单本判断直接读 cover。


## 四、数据源与抓取规范

### 1. 豆瓣（元数据 + 简介 + 封面 + 评分）

- **Google Books API 与 Open Library 在大陆网络不可用**，默认数据源为豆瓣。
- 搜索：`https://book.douban.com/j/subject_suggest?q=关键词`（返回候选 JSON）。
- 详情：`https://book.douban.com/subject/{id}/`，解析出版社、出版年、ISBN、丛书、简介、评分（`property="v:average"`）。
- 封面：候选返回的 `pic` 字段 `s/` 换成 `l/` 取大图；请求必须带 `User-Agent` 和 `Referer: https://book.douban.com/`（否则返回 418）。

### 2. 微信读书（评分）

评分数据源（接口、鉴权、字段解析、版本匹配、算法）：**见同目录《微信读书Skill.md》**。

### 3. 通用规则

- 请求间隔 ≥1 秒，失败重试（2.5s 递增）。
- 拼音/英文/繁体标题需映射为可检索关键词（如 `Niu nai ke le jing ji xue` → `牛奶可乐经济学`，`醫生也在讀的癌症真相` → `癌症真相`）。

## 五、格式规则

- 同一本书同时存在 PDF 与其他格式（azw3/mobi/epub）时，**删除 PDF**，保留 Kindle 友好格式。
- 只有 PDF 的书保留，**PDF→azw3 转换是用户自己的操作**，不代做。
- 删除格式用 `calibredb remove_format <id> <format>`，不直接操作文件系统删除。
- 不触碰 `.calnotes/`（用户笔记）、`.caltrash/`（Calibre 回收站）中的内容。

### 漫画卷目录整理（消去中间文件夹，用户明确要求）

漫画书源常按卷拆成多个子文件夹，每个卷文件夹里只有书文件，卷文件夹本身无保留价值。整理目标：**把文件上移到父目录，删除空的卷文件夹**。

流程：

1. 列出目标目录树，核对结构（父目录 + N 个卷子文件夹 + 文件），先给出清单；
2. 逐个卷文件夹，将其中文件移动到父目录；
3. **同名冲突不覆盖**：保留双方（改名加卷号后缀）或停下来问用户；
4. 确认子文件夹**完全为空**后才删除，禁止未确认即删；
5. 不触碰 `.calnotes/`、`.caltrash/` 等用户内容；
6. 完成后报告：移动文件数、删除文件夹数、冲突处理结果。

## 六、常用命令速查表

| 操作 | 命令 |
|---|---|
| 列出全部书 | `calibredb list --with-library <库> --fields=id,title,authors,series,tags,comments,publisher,pubdate,isbn,languages,rating --for-machine` |
| 看单本元数据 | `calibredb show_metadata --with-library <库> <id>`（加 `--as-opf` 看 OPF 结构）|
| 写元数据 | `calibredb set_metadata --with-library <库> <id> --field=字段:值 [--field=…]` |
| 嵌入文件 | `calibredb embed_metadata --with-library <库> <id>` |
| 删除格式 | `calibredb remove_format --with-library <库> <id> pdf` |
| 看文件内嵌元数据 | `ebook-meta <文件路径>` |
| 提取文件内嵌封面 | `ebook-meta <文件> --get-cover <输出.jpg>` |
| 查图片分辨率 | `sips -g pixelWidth -g pixelHeight <图片>` |

## 七、异常处理表

| 现象 | 处理 |
|---|---|
| `calibredb` 报“另一个 calibre 程序正在运行” | Calibre GUI 开着，告知用户关闭后重试 |
| 豆瓣 429/限流/返回空 | 间隔加长（3-5s）重试 3 次；仍失败 → 该字段留空，报告说明 |
| 豆瓣封面 418 | 补 `User-Agent` + `Referer: https://book.douban.com/` |
| 微信读书 `errcode ≠ 0` | 按回包中文提示处理；仍失败 → 不写评分，报告说明 |
| **ISBN 与书不符 / 版本对不上** | **不硬填，留空，写入报告**（书名 + ISBN 冲突说明）|
| `set_metadata` 部分失败 | 记录失败 id，修复后从失败 id 续跑（见八）|
| 目录被意外重命名 | 停止操作，立即告知用户，不自行回滚 |

## 八、批量处理与幂等

- `calibredb set_metadata` / `embed_metadata` 是**覆盖写**，天然幂等：重复执行结果一致，失败重跑无害。
- **已处理判定**（幂等键）：标签非空 && 简介 ≥50 字 && 评分非空 → 视为已处理，跳过（支持断点续跑，不重复劳动）。
- **断点续跑**：按 id 升序处理；单本失败记录并继续下一本；重试时从失败 id 开始，不重头跑。
- 单本操作不走批量流程，直接执行。

## 九、验收流程（每次写库后必须执行）

1. `calibredb list` 核对字段（丛书/标签/简介/出版社/日期/ISBN/语言/评分）是否写入；
2. `ebook-meta <文件>` 抽查 1-3 本，确认文件内嵌元数据已更新；
3. 封面检查：`sips -g pixelWidth -g pixelHeight` 确认已替换封面的分辨率达标；
4. 向用户报告：每本改了什么、哪些信息没找到、遗留风险。

## 十、Skill 路由

| 任务 | 主 Skill | 说明 |
|---|---|---|
| 微信读书评分 | `weread-skills` | 接口与算法详见《微信读书Skill.md》 |
| 元数据补全 / 豆瓣抓取 / 封面替换 / 新增书处理 | （待封装 `calibre-metadata`） | 未来封装后在此填写路由 |

目前除微信读书评分外，其余任务按本文件规则直接执行，不依赖外部 Skill。

## 十一、新增书处理 Checklist（标准流程）

```text
1. 扫描：calibredb list 对比现有，识别新增书籍（id 或元数据完整度）
2. 检查格式：是否有 PDF 重复（有则 remove_format）
3. 豆瓣抓取：suggest 选正确版本条目 → 详情页抓取（出版社/日期/ISBN/丛书/简介/评分），仅作参考素材
4. 微信读书抓评分：接口与算法详见《微信读书Skill.md》，按其中规则计算 rating（回退链见三.6）
5. 丛书识别：按三.3 规则——先读内容证据（书名卷册结构/简介/系列线索）自行归类；漫画多卷本按三.3c 识别；豆瓣丛书字段仅作佐证
6. 生成元数据：标签、简介（重写）、出版社/日期/ISBN；标题带营销长尾 → 按三.7 清理
7. 检查封面：无封面或低清(<400px)或不合审美 → 按三.10 下载豆瓣大图替换；**漫画直接用书内封面**
8. 备份 metadata.db
9. 写入：set_metadata（含 --field=cover 换封面、--field=rating 评分）→ embed_metadata 嵌入文件
10. 校验：calibredb list + ebook-meta 抽查
11. 清理中间文件（/Users/eiravale/Desktop/临时文件/）
```

注：漫画类书籍跳过步骤 3-4（不做外部数据源写入），rating 留空，丛书按三.3c，封面用书内封面。

## 十二、报告格式

处理完成后交付 Markdown 报告（兼容 Typora）：

- 变更总览（表格：书名 / 改了什么 / 缺什么）；
- 数据质量问题（作者错填、标题污染、封面错误、**ISBN 不符**等）；
- 评分情况（微信读书分 / 豆瓣分 / 计算后 rating，缺源的书注明）；
- 需要用户决策的事项（含丛书判断存疑的书）；
- 备份位置与清理说明。
