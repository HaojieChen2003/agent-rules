# GitHub 操作手册

本文件记录 `/Users/eiravale/Desktop/Agent Rules/` 规则库的 Git 与 GitHub 维护方法。它是仓库操作手册，不参与日常领域任务路由。

## 仓库信息

| 项目 | 当前值 |
|---|---|
| 本地仓库 | `/Users/eiravale/Desktop/Agent Rules/` |
| 远程仓库 | `https://github.com/HaojieChen2003/agent-rules` |
| 默认远程与分支 | `origin/main` |
| 可见性 | 公开仓库 |
| 本地提交身份 | 仓库级 `user.name=eiravale`，不修改全局配置 |

仓库于 2026-08-15 初始化。历史事实以 `git log` 和 GitHub 当前状态为准，不把提交数、文件数或旧 commit 固化为行为规则。

## 修改前检查

```bash
git status --short
git log --oneline -8
git remote -v
```

先识别未提交修改。与当前任务无关的改动均视为用户内容，不覆盖、不暂存、不提交。读取规则时以当前工作树内容为准；`git diff` 可辅助了解近期变化，但不能依赖跨任务记忆，也不能用差异片段代替理解当前生效文件。

## 本地修改流程

```text
读取当前版本 → 明确单一修改主题 → 编辑必要文件 → 检查引用、版本和格式
→ 查看 diff → 只暂存本次相关文件 → 本地提交
```

修改领域框架时更新其 `version` 和 `last_updated`。仅修改说明文档或总入口时，不为无关领域框架机械升级版本。

版本号按 `x.y.z` 从最后一位 `z` 开始迭代：常规规则增删改只递增 `z`（如 1.0.0 → 1.0.1），不跳级直接改前一位；**最后一位满 10 时进一位**（如 1.0.9 → 1.1.0，1.1.9 → 1.2.0），满 10 前不得提前进位。确需结构性升级时先说明理由并取得用户同意。用户明确要求的版本策略优先于本默认约定。

推荐提交信息为 `<范围>: v<版本> <摘要>`，例如：

```text
latex: v1.8.0 新增语义下标字体规范
agents: 精简入口并下沉 GitHub 操作细节
```

一次提交应围绕一个可解释的主题；多个文件共同完成同一次架构调整时可以放在同一提交中。

## 检查与提交

```bash
git diff --check
git diff --stat
git diff -- <相关文件>
git add -- <相关文件>
git diff --cached --check
git diff --cached
git commit -m "<提交信息>"
```

不要使用会顺带暂存无关文件的宽泛命令。提交后用 `git status --short` 和 `git log -1 --oneline` 核对结果。

## 推送与同步

`git commit` 只改变本地仓库；`git push` 会改变 GitHub 远程状态。**未获得用户明确同意前，绝不执行 push**——即使本地存在未推送提交，也不得自行推送；只能告知用户存在待推送提交并等待授权。推送前必须向用户说明将推送的分支、主要提交内容和目的，只有获得明确同意后才能执行：

```bash
git push origin main
```

拉取更新前先检查工作树。若存在本地修改，不机械执行可能触发合并的操作；先判断能否安全同步。常规只读检查：

```bash
git fetch origin
git status -sb
git log --oneline --left-right HEAD...origin/main
```

## 恢复与回滚

恢复前先确认目标文件、提交和影响范围。优先使用保留历史的方式：

- 查看文件历史：`git log --oneline -- <文件>`；
- 查看历史内容：`git show <commit>:<文件>`；
- 撤销已提交变更：`git revert <commit>`；
- 恢复工作树文件前，确认该文件没有需要保留的用户改动。

不得使用会清空整个工作树或改写共享历史的破坏性命令，除非用户明确要求并已确认精确影响。

## 公开仓库注意事项

提交或推送前检查 API Key、Cookie、Token、身份信息、本机隐私路径、数据库、书籍、论文原稿、临时日志、构建产物和大文件。示例中不得包含真实凭据。

敏感信息一旦进入公开 Git 历史，仅删除工作树文件并不足以消除泄露；发现后应立即停止推送并报告。

## GitHub CLI

可使用 `gh` 做只读检查；创建仓库、修改可见性、创建发布、合并 PR、删除远程分支等外部状态变更需要用户授权。命令和平台行为可能变化，遇到不确定选项时以当前 Git/GitHub 官方帮助为准。
