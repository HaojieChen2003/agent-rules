---
when_to_use:
  - 已有 LaTeX 工程的编译、排版、引用、公式环境和安全编辑
  - 中文学位论文格式、盲审、学校规范和 GB/T 7714
  - Beamer 演示文稿的结构、主题和视觉验收
do_not_use:
  - 纯数学结论、证明或数值验证
  - 只有 DOCX 或 PDF 而没有 LaTeX 源文件
  - 从零代写论文内容
version: 1.8.0
last_updated: 2026-08-17
---
# LaTeX 排版助手

处理已有 LaTeX 工程中的定向任务，以安全编辑、工具链兼容、语义清晰和可验证产物为目标。普通 LaTeX、Beamer 和学位论文共享工程原则，但只加载当前任务需要的专项规则。

## 定位与接口

- 数学结论、证明、符号推导和数值验证由 `数学工作者.md` 主导；本框架不以“能够编译”替代数学正确性。
- 编译、公式环境、引用、字体语义、版面和工程结构由本框架主导。
- 数学内容写入 `.tex` 时，先验收数学内容，再安全写入、编译并检查 PDF；分别报告两类结果。
- 普通数学笔记和 Beamer 不得机械套用学位论文结构、盲审、学校规范或语言审校流程。

## 任务路由

以下参考文件位于 `references/latex/`。命中相应任务时完整读取所列文件，不加载无关模块。

| 任务 | 必读参考 | 最低验收 |
|---|---|---|
| 编译失败、工具链或缓存问题 | `compile.md` | 找到首个阻断性根因；目标产物生成；无新增未定义引用 |
| 公式、符号、上下标和数学字体编辑 | `math-typography.md` | 语义字体一致；数学环境合法；公式可读且不越界 |
| 批量替换或结构性编辑 | `safe-editing.md` | 修改范围准确；特殊环境未受损；残留扫描和编译通过 |
| Beamer 幻灯片 | `beamer.md` | frame 结构完整；无内容溢出；主题装饰符合任务要求 |
| 中文学位论文 | 当前安装的 `latex-thesis-zh` Skill | 学校规范、文献、盲审和结构按任务逐项验收 |

## 核心原则

1. **项目优先**：沿用现有文档类、编译器、文献系统、目录、宏、标签、字体和设计语言；用户未要求改版时不擅自更换。
2. **最小充分流程**：根据项目实际依赖选择编译步骤，不对无文献项目机械运行 Biber，不默认启用 `shell-escape`。
3. **内容与产物分开验收**：编译成功不代表数学、引用、编号、术语或视觉版面正确。
4. **根因优先**：先定位第一个阻断性根因，检查最近修改与缓存状态，不用无关改动掩盖错误。
5. **安全编辑**：批量修改必须限定匹配范围、保护特殊环境、写盘前核对命中数量，并在写盘后扫描残留和编译验证。
6. **语义排版**：数学字体表达对象的数学角色，不仅追求外观统一；变量、说明性文字、算子、集合和单位应使用相应字体。
7. **产物边界**：按项目约定区分正式源文件、最终产物、可再生成中间文件和增量构建缓存；只清理本任务创建且已确认可再生成的内容。

## 数学字体准则

数学模式默认把字母解释为变量并使用斜体。由多个字母组成、表示名称、类别、缩写或说明文字的上下标不应被误排成变量乘积。

- 单字母变量下标保持数学斜体：`a_i`、`u_h`、`x_k`。
- 说明性或语义性下标使用直立罗马体：`c_{\mathrm{cap}}`、`p_{\mathrm{in}}`、`T_{\mathrm{ref}}`、`u_{\mathrm{max}}`。
- 完整自然语言说明优先使用 `\text{...}`：`E_{\text{初始}}`、`u_{\text{边界}}`；英文短标签可按项目约定统一使用 `\mathrm{...}` 或 `\text{...}`，同类对象不得混用。
- 标准函数和算子使用已有命令或 `\operatorname`，不用普通斜体拼写：`\sin`、`\exp`、`\operatorname{rank}`。
- 数集和固定结构按语义使用黑板粗体或项目约定字体：`\mathbb{R}`、`\mathbb{N}`；向量和矩阵的粗体方案沿用项目既有约定。
- 单位使用直立体并与数值保持合理间距；项目使用 `siunitx` 时优先沿用其接口。

编辑时先判断下标是“变量”还是“标签”，不得对所有下标机械添加 `\mathrm`。完整规则见 `references/latex/math-typography.md`。

## 默认环境

项目已有工具链优先。没有既有约定时，中文文档通常使用 XeLaTeX；使用 `biblatex` 时配合 Biber。中文支持优先使用 CTeX 文档类或 `ctex` 宏包，避免重复加载；保留项目已有字体选择。

## 验收

修改后根据风险检查：

1. 编译命令退出状态正常，目标 PDF 成功生成；
2. 没有新增未定义引用、标签、文献或控制序列；
3. 自定义命令、数学环境、引用键和目录结构未被意外破坏；
4. 数学字体、上下标、算子和单位的语义一致；
5. 关键页面无不可接受的溢出、遮挡、截断或层级混乱；
6. 临时产物已按精确范围处理，正式源文件和必要缓存未被误删；
7. 最终报告修改内容、验证方式、编译结果、仍存在的警告和未覆盖范围。

## 边界

- 不把普通 `.tex` 文件自动视为学位论文。
- 不以全局格式化或大规模重写代替局部修复。
- 不未经验证修改文档类、文献系统、字体体系或全局主题。
- 不把具体软件版本、单次事故、固定行号或单个项目经验提升为普遍规则。
