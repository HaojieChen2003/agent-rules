---
when_to_use:
  - 飞书博客/日记内容转 LaTeX 排版（BlogDiary 模板及衍生工程）
  - 长文流水账 + 大量图片的轻量 LaTeX 工程
  - 日记模板的维护、扩展与性能优化
do_not_use:
  - 纯数学结论、推导、证明或数值验证
  - 学术论文、数学笔记、Beamer 的排版规范
  - 日记内容的撰写、润色或情感分析
version: 1.1.0
last_updated: 2026-08-19
---
# 日记排版助手

处理飞书博客、个人日记类内容转 LaTeX 排版的任务，面向"性能优先 + macOS 风格 + 大量图片"的长期日记工程。模板与转换器都是已固化的可复用工具，本框架只沉淀排版层的决策与验收。

## 定位与接口

- 模板：`TexStudio/Templates/TemplateBlogDiary/BlogDiaryTemplate.tex`。
- 工程目录：`TexStudio/Blog/`
  - `Blog.md`：飞书导出的源 Markdown（不要手工改，改了会被 `md2tex.py` 覆盖）。
  - `BlogDiary.tex` / `BlogDiary.pdf`：转换器产物（自动生成，勿手工长期维护）。
  - `图片和附件/`：原图（PNG/JPEG，**永不被修改**，可逆）。
  - `图片和附件/opt/`：**优化后 JPEG**（降采样到 ≤1600px 宽 + q85），由 `build/optimize_images.py` 生成，可整体删除重生成。
  - `build/md2tex.py`：Blog.md → BlogDiary.tex 转换器（处理日期标题、段落、引用、**fenced 代码块** ```` ``` ````、行内 `` `code` ``、并排图片、表格、列表、公式、链接；图片路径映射到 `opt/`）。
  - `build/optimize_images.py`：图片预优化（按 mtime 缓存，仅重处理变更新图）。
  - `build/build.sh`：一键重建 = 优化图片 → md2tex → xelatex；默认 1 遍（编辑快编），`--export` 2 遍（导出终稿）。
- 学术 LaTeX 工程（数学笔记、论文、Beamer）由 `Latex排版助手.md` 主导；本框架只处理日记类流水账排版。
- 数学内容写入时，内容正确性由 `数学工作者.md` 负责，本框架只负责安全写入、编译与版面验收。

## 核心原则

1. **性能优先**：禁止 tcolorbox/tikz/biblatex——编译时间随图片数线性增长，日记会写数年。`wrapfig`、`needspace`、`seqsplit` 属 CTAN 标准轻量包，允许。
2. **图片三件套**：所有 `\includegraphics` 必须同时写 `width` + `height` + `keepaspectratio`。单图 `height≤0.55\textheight`，并排图每张 `≤0.5\textheight`。理由：长截图 h/w 可达 2.2（1264×2800），只限宽会让高度超过整页。
3. **断页纪律**：图片前加 `\filbreak`（剩余空间不足时自动换页，避免图片被切在页边）；`\wphoto` 前必须 `\par\needspace{0.35\textheight}`——wrapfig 在页面底部剩余空间不足时**不会自动断页**，无此防护会直接越界（PyMuPDF 可检出 y1>页高）。
4. **macOS 风格**：正文 Songti SC、标题 PingFang SC（Semibold）、西文 Helvetica Neue；mac 色板（macText/macSecondary/macSeparator/macBlue 等）；圆角卡片感用 `\rule`+`minipage` 实现，不引 tcolorbox。
5. **日期三段式**：`\entry{日期}{地点}{天气}` = macBlue 4.5pt 竖条 + 1.6em 间距 + minipage 内 LARGE 日期 + secondary 灰色地点/天气小字 + 灰线分隔；一级标题用 `\maintitle`（H1 居中大号 + 灰线）。地点/天气为空传 `{}` 自动跳过。
6. **图文混排**：单图且后跟文字段落自动用 `\wphoto[0.45]`（wrapfigure 右环绕 0.45\textwidth，限高 0.42\textheight）；多图组用 `\photoset` 整块居中（`\@for` 自动均分宽度）。
7. **emoji 限制**：xelatex+ctexart 无彩色 emoji 字体（XeTeX 字体后端不支持 .ttc 彩色字形 COLR/CPAL；LuaLaTeX 可行但代价大）。用颜文字（开心）/（哭哭）或 `$\bigstar$`；表格评级 ★（U+2605）直接转 `$\bigstar$`，不能依赖宋体覆盖。
8. **图片命名**：文件名含空格必须改为下划线（`截屏2026-05-30 20.54.09.png` → `_20.54.09.png`），md 引用同步替换；转义写法 `\![..](..)` 中的 `\ ` 也要还原。

## 构建管线与性能

- **图片预优化是提速关键**：原图 253MB PNG（截图为主）直接嵌入时，单遍编译 ~43s、PDF 233MB。降采样到渲染分辨率 + JPEG q85 后：265MB → opt 63MB，**单遍 43s → 1.3s（约 33×），PDF 233MB → 61MB（约 3.8×）**，双遍导出仅 2.6s。瓶颈是 PNG 解码，JPEG 几乎可忽略。
- **编辑 1 遍 / 导出 2 遍**：日记无 `\ref`/`\cite`/目录，跨页引用不重排，编辑期跑 1 遍即可看（1.x s）；仅 `--export` 终稿跑 2 遍稳定书签/链接。这是零风险的首个提速手段。
- **预编译导言（mylatexformat）结论：不做**。实测纯导言（字体+宏包加载）仅 ~0.4s，优化后单遍已 1.3s；预编译对 xelatex+fontspec+中文/hyperref 有踩坑风险，收益可忽略，故跳过（build.sh 的 `--fmt` 分支已删）。
- **增量重生成**：`optimize_images.py` 按 mtime 比对只重处理变更新图；`md2tex.py` 每次全量重生成 `.tex`（纯文本，快）。
- **已知限制（非 bug）**：飞书视频/富媒体无法嵌入 LaTeX，转换时静默丢失（应告知用户）；彩色 emoji 仍只能颜文字/$\bigstar$。

## 常见坑（均有案例）

- **引用块 Underfull**：`\q` 内以 `\\` 结尾（尾部空行）或短行被 `\\` 强制断行，产生 `Underfull \hbox badness 10000`。修法：尾部空行 pop 掉；段间用 `\par` 不用 `\\`。
- **`\entry` 竖条布局**：`\noindent\rule` + minipage 组合，实测无 underfull，勿再改回单参数版。
- **md2tex 跨行 `$...$` 公式**：首行 `$` 尾行 `$` 必须整块识别为数学，否则 `^`/`_` 被 tex_escape 误转义。
- **`\href` 后裸 URL 二次正则**：会无限嵌套导致栈溢出，用负向断言 `(?<!\href{)`。
- **`<br>` 转 `\\`**：不能带前后空格，否则变成防断空格 `\ `。
- **`unescape` 顺序**：先保护 `\\`（双反斜杠）再处理单反斜杠转义。
- **邮箱/长 token**：用 `\seqsplit` 包裹断行。

## 最低验收

- 导出终稿（`--export` 2 遍）须 XeLaTeX×2，Error / Underfull / Overfull / Missing character 全部为 0；编辑快编（1 遍）允许，但交付前须跑一遍 `--export` 复核零警告。
- PyMuPDF 扫描：无任何图片 bbox 越出页面边界。
- 图片数量与分布符合源内容（无重复嵌入、无漏图）。
- 模板示例（BlogDiaryTemplate.tex）同样零警告——示例图片调用应写成注释，不依赖样例图文件。

## 参考资料

- 模板：`TexStudio/Templates/TemplateBlogDiary/BlogDiaryTemplate.tex`
- 转换器：`TexStudio/Blog/build/md2tex.py`（Blog.md → BlogDiary.tex，含 fenced 代码块处理）
- 图片优化：`TexStudio/Blog/build/optimize_images.py`（→ `图片和附件/opt/`）
- 一键重建：`TexStudio/Blog/build/build.sh`（`--export` 双遍导出）
- 实例：`TexStudio/Blog/BlogDiary.tex`（935 行）/ `BlogDiary.pdf`（50 页 / 61MB，165 图全量嵌入）
- 图片尺寸分析：venv 环境 `/Users/eiravale/.workbuddy/binaries/python/envs/default/bin/python` 已装 pymupdf（fitz）
